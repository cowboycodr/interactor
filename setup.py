from setuptools import setup, find_packages

setup(
    name="api-interactor",
    version="0.0.1",
    description="",
    packages=find_packages(),
    keywords=[],
    install_requires=['choam', 'wheel', 'requests', 'twine'],
    project_urls={
        'Source': 'https://github.com/cowboycodr/interactor'
    },
)